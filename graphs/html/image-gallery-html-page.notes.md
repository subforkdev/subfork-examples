# Image Gallery HTML Page

Purpose: sketches a static image gallery page without requiring provider keys or published graph IDs.

Current behavior: uses static image metadata and renders a responsive gallery through the HTML Template node.

Architecture note: later this can swap the static gallery JSON for outputs from composite image-generation nodes.
